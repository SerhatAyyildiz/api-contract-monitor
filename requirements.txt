# Projenin ihtiyaç duyduğu dış kütüphaneler (hazır kod paketleri).
# Kurmak için: pip install -r requirements.txt

# İnternet üzerinden API'lere istek atmak için
requests==2.32.3

# Gizli bilgileri .env dosyasından okumak için
python-dotenv==1.0.1

# Otomatik testleri çalıştırmak için
pytest==9.1.1
